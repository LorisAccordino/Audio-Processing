namespace AudioProcessing.Common
{
    public enum AudioChannel
    {
        Stereo,
        LeftChannel,
        RightChannel,
    }

    public enum TimeFormat
    {
        Samples,
        Seconds,
        Milliseconds,
        Total
    }
}
